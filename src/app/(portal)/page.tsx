export default function OverviewPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold mb-4">Overview</h1>
      {/* TODO: Global KPIs, heatmap, alert banners, recent runs, suite health cards */}
    </div>
  );
}
